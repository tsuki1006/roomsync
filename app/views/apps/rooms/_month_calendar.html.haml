= month_calendar events: schedules do |date, schedules|

  = link_to url_for(start_date: date, about: date), data: { turbo_stream: true } do
    = date.day
    - count = todays_schedule_count(schedules, date)
    - if count.present?
      .calendar_schedule_count= count
